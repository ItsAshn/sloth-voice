{
  "name": "sloth-voice",
  "version": "0.1.0",
  "description": "Sloth Voice — locally-hosted Discord alternative",
  "private": true,
  "scripts": {
    "server": "cd server && npm run dev",
    "desktop": "cd desktop && npm run dev",
    "mobile": "cd mobile && npx expo start",
    "client": "cd client && npm run dev",
    "install:all": "npm i --prefix server && npm i --prefix desktop && npm i --prefix mobile && npm i --prefix client",
    "dev": "npx concurrently -n server,desktop -c blue,green \"npm run server\" \"npm run desktop\"",
    "release": "node scripts/release.mjs"
  }
}
