{
  "name": "sloth-voice",
  "version": "0.8.5",
  "description": "Sloth Voice — self-hosted Discord alternative (desktop + mobile)",
  "private": true,
  "scripts": {
    "desktop": "cd desktop && npm run dev",
    "mobile": "cd mobile && npx expo start",
    "install:all": "npm i --prefix desktop && npm i --prefix mobile",
    "dev": "cd desktop && npm run dev",
    "release": "node scripts/release.mjs"
  }
}
